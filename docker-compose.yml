version: '3.0'

services:
  nginx:
    build:
      context: ./docker/nginx
    volumes:
      - ./:/site
      - ./logs:/var/log/nginx
      - /etc/letsencrypt/live:/etc/letsencrypt/live
    container_name: lock-screen-nginx
    ports:
      - "80:80"
      - "443:443"